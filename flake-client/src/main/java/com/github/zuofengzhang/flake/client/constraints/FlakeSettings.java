package com.github.zuofengzhang.flake.client.constraints;

import java.util.concurrent.TimeUnit;

public class FlakeSettings {
    public static long timeInSeconds = TimeUnit.SECONDS.toSeconds(20);
}
